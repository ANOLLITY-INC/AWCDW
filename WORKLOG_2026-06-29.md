# WORKLOG 2026-06-29 (작업 기간: 2026-06-24 ~ 06-29)

AWCDW 워크숍 포털(정적 HTML + Firebase) 작업 내역 정리.
프로젝트: `amcdw-9d10e` / Firestore DB: `database1` / 호스팅: https://amcdw-9d10e.web.app

---

## 0. 한눈에 보기
- 사용자 관리 ↔ 학생 팀배정 동기화 (지원자 명단 기준 포함)
- 팀 관리 페이지에 지도교수 표시 + "사용자 관리 기준 동기화" 버튼
- **워크숍 차수별 진행 결과 발표** 보드 신설(작품 보드와 병렬, 권한·단계 분리)
- 부산 숙소 호실·비밀번호 표시 + 방 재배정(B→어반스테이 S/SR)
- **팀 배치 관리**(관리자) / **팀 배치 현황**(전체 공개) 페이지 신설
- 지도교수 명단 공개 토글(기본 비공개)
- 공학생 팀 변경 버그 수정(지원 문서 동기화)
- 1차 워크숍(부산) 일정 일부 수정

> 참고: 팀/지원/설정 등 **라이브 Firestore 데이터 변경은 관리자 UI(버튼)로 수행**한다.
> 작업 환경에서 운영 DB 직접 쓰기는 불가(보안규칙상 관리자 세션 필요).

---

## 1. 학생 팀배정 동기화 (`js/auth.js`)
**문제:** 팀원 배정(이름)·지원(공학생)과 `users.teamId`가 따로 놀아 사용자 관리에서 "미배정"으로 보임.

- `reconcileTeamIds(teams, users, apps)` — 각 학생이 가져야 할 `teamId` 계산.
  - 우선순위: **지원자 명단(`applications.assignedTeamId`, uid 기준) > 팀 명단(`teams.members`, 이름 기준)**.
  - 교수·관리자는 제외.
- `syncAllTeamIds()` — teams/users/applications 읽어 일괄 반영.
- **사용자 관리 헤더에 「↻ 팀 배정 동기화」 버튼**(`runTeamSync`).
- 팀 저장/삭제 시 자동 동기화(`saveTeam`/`deleteTeam`).
- `syncUserNameIntoTeams()` — 사용자 관리 드롭다운으로 팀 바꾸면 `teams.members`도 양방향 갱신.
- 사용자 관리 표에 `teamId` 비었지만 명단에 있으면 **"미동기화"** 배지 표시.

## 2. 팀 관리 페이지 (`js/auth.js`)
- 표에 **"지도 교수" 열** 추가 — `users.advisingTeamIds` 기준 실시간 표시(`teamAdminUsers`).
- **「↻ 사용자 관리 기준 동기화」 버튼**(`syncTeamsFromUsers`):
  - 각 팀 `members` = 그 팀 소속(`teamId` 일치) **디자이너 + 공학생** 이름(디자이너 먼저, 공학생 다음).
  - `advisingProfessors`도 함께 최신화.

## 3. 워크숍 차수별 진행 결과 발표 보드 (`js/works.js`, `js/auth.js`, `index.html`, `firestore.rules`)
작품(사전주제제안) 시스템을 `category` 필드로 병렬 확장.
- `workCtx.cat`: `"proposal"`(기본) | `"result"`. history에도 보존.
- 진입: `openProposalBoard()` / `openResultsBoard()`. 메인 카드 + 각 역할 대시보드 메뉴 추가.
- 권한(`canUploadCat`/`canManageWork`):
  - proposal: 소속 팀 **디자이너**.
  - result: 그 팀 배정 **학생(디자이너·공학)** + **담당 교수**(`advisingTeamIds`).
- `loadTeamWorks` 카테고리 필터, `saveWork`가 `category` 기록.
- 게시 단계(phase):
  - proposal: 중간제출 / 최종제출
  - result: **1차 / 2차 / 3차 워크숍** (라벨 "워크숍 차수")
- **firestore.rules** `works` 갱신:
  - 읽기: public 또는 (관리자·교수·그 팀 배정 학생).
  - 생성/수정/삭제: proposal=소속 팀 디자이너 / result=배정 학생·담당 교수·작성자·관리자.
  - 카테고리 안전 접근: `request.resource.data.get('category','proposal')`.

### 3-1. 결과 보드 팀 보드 = 팀 배치 현황 기준 (`js/main.js`, `js/works.js`)
- 결과 보드에서 팀 카드를 `renderResultTeamsBoard`로 렌더: **팀원(members) + 지도교수(공개 시)**, 지원 정원/배너 제거, 클릭 시 결과 상세.
- 상단 **통계(Total Teams/Designers/Avg) 숨김**(결과 보드 한정, `#teams-stats` 토글).
- 사전주제제안(파랑)과 구분되도록 **짙은 배너 + 앰버 포인트** 색상 적용.

## 4. 부산 숙소 호실·비밀번호 (`js/busan-info.js`, `firebase.json`)
출처: `부산 숙소 배정표.jpg`(어반스테이 해운대역). 타입 매핑 **A=ST, C=S, D=SR**(B=W레지던스, 표에 없음).
- `URBANSTAY_ROOM_DETAIL`(키 `dorm-room` → `{호실, 비밀번호}`), `URBANSTAY_ELEVATOR_PW = '7878#'`.
- 이름 검색 시 **호실 + 객실 비밀번호** 강조 박스, **엘리베이터 비밀번호는 상단 별도 칸**.
- 비밀번호 담긴 배정표 이미지는 `firebase.json` ignore 처리(호스팅 비공개).

### 4-1. 방 재배정 (2026-06-25)
기존 **B타입(W레지던스) 12명 중 황세현만 B 유지**, 나머지 11명을 어반스테이로 이동(순서 임시배정 — **검수 필요**):
- S타입(C-9~14): 임창현 703 / 장승원 1401 / 김태호 1415 / 여환철 1505 / 허지웅 807 / 이하늘 918
- SR타입(D-4~8): 정운영 1412 / 박도현 706 / 최강 816 / 함대희 906 / 조민재 912
- 검증: 어반스테이 38명 전원 호실·비번 매핑, 호실 충돌 없음. 숙소 미이용 9명(조지운+부산대 8명)은 대상 외.

## 5. 팀 배치 관리 (관리자) (`js/auth.js`, `index.html`, `js/main.js`, `firestore.rules`)
- 새 관리자 페이지 `team-place`: 팀별 **디자이너·공학생(지원)·지도교수** 한 화면.
- **미배정 학생 배정**(`placeStudent`): 공학생=지원 문서 생성/갱신, 디자이너=팀 명단 추가, + `users.teamId`.
- **「계정 없는 학생 직접 추가」**(`addNameToTeam`): 이름만 팀 명단에 추가.
- **지도교수 공개 토글** 배치(아래 7).
- firestore.rules: `applications` 생성에 **관리자** 허용 추가(공학생 직접 배정용).

## 6. 팀 배치 현황 (전체 공개) (`js/auth.js`, `index.html`, `js/main.js`)
- 공개 읽기 전용 페이지 `team-status`(메인 카드): 팀별 디자이너 + 공학생(로그인 시) + 지도교수(공개 시).
- 일반 사용자는 `users`를 못 읽으므로 교수 명단을 공개 읽기 가능한 **`teams.advisingProfessors`에 비정규화**(`rebuildTeamProfessors`).

## 7. 지도교수 명단 공개 토글 (`js/apply.js`, `js/auth.js`)
- `settings/app.professorsPublic` (기본 **비공개**), `professorsPublic` 전역 + `toggleProfessorsPublic()`.
- **팀 배치 관리 상단 「지도교수 공개/비공개」 버튼**.
- 공개 시에만 `rebuildTeamProfessors`가 `teams.advisingProfessors` 채움(비공개면 비움) → 공개/결과 보드에 노출 제어.

## 8. 공학생 팀 변경 버그 수정 (`js/auth.js`)
**문제:** 공학생 소속을 사용자 관리에서 바꿔도 동기화 시 지원 문서(옛 팀)로 되돌아감("어떻게 해도 변경 안 됨").
**수정:** `saveUserAdmin`에서 **공학생 팀 변경 시 `applications.assignedTeamId`도 함께 갱신**(미배정이면 지원 문서 삭제).
- 참고: B+H팀의 실제 팀 id는 `"B"`(이름만 "B+H팀").

## 9. 1차 워크숍(부산) 일정 (`index.html`)
- **Day 2(06.25)**: 13:00~15:00 ↔ 16:00~18:00 내용·장소 맞바꿈.
  - 13:00~15:00 = 팀별 활동: 기초 폼보드 목업 제작(A-Space·C-Space)
  - 16:00~18:00 = 팀별 발표 및 지도교수단 피드백(1층 114호)
- **Day 2·Day 3 맨 위에 "서명부 작성 후 대기 (1층 114호)"** 행 추가(시간 `-`).

---

## 배포 / 캐시
- 매 변경 시 `firebase deploy --only hosting`(필요 시 `firestore:rules` 포함)로 배포.
- JS 캐시버전(`?v=NN`) 증가로 갱신 강제. 현재: auth v29, works v27, apply v26, busan-info v27, main v27.

## 남은 일 / 주의
- **검수 필요:** 부산 숙소 호실·비밀번호(순서 임시배정).
- **관리자 클릭 필요(라이브 데이터):** 팀 관리 「사용자 관리 기준 동기화」, 팀 배치 관리 「지도교수 공개」, 팀 배정 동기화 등.
- 결과 보드 팀원/교수 표시는 위 동기화·공개가 선행돼야 최신으로 보임.
- 공개 보드의 "디자이너" 라벨에 공학생이 합산될 수 있음(팀원 명단에 공학생 포함 정책).
